export { log } from "./log";
import {
  isPossiblePhoneNumber,
  isValidPhoneNumber,
  parsePhoneNumber,
} from "./phoneValidator";

export const utils = {
  isPossiblePhoneNumber,
  isValidPhoneNumber,
  parsePhoneNumber,
};
